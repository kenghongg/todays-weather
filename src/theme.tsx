import { createTheme } from "@mui/material";

const theme = createTheme({
  palette: {
    mode: "dark",
    // primary: { main: "#28124D" },
    secondary: {
      main: "#28124D"
    }
  },
  typography: {
    fontFamily: 'Noto Sans'
  }
});

export default theme;
